<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	<select id="selectList" parameterType="int" resultType="com.javaex.vo.BoardVo">
	<![CDATA[
         select no,
                title,
                content,
                hit,
                reg_date regDate
         from board
         order by no desc	
	]]>
	</select>

</mapper>
